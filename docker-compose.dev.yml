name: prohub-dev

services:
  rembg:
    image: danielgatis/rembg:latest
    container_name: prohub-rembg-dev
    command: ["s"]
    ports:
      - "8081:7000"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G

  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: prohub-backend-dev
    working_dir: /app/server
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: development
      PORT: 3000
      CHROME_PATH: /usr/bin/chromium-browser
      HEADLESS: "true"
      REMBG_URL: http://rembg:7000
      REMBG_MODEL: isnet-general-use
      SMTP_HOST: ${SMTP_HOST:-smtp.qq.com}
      SMTP_PORT: ${SMTP_PORT:-465}
      SMTP_SECURE: ${SMTP_SECURE:-true}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      FEEDBACK_FROM: ${FEEDBACK_FROM:-}
      # ---- 测试隔离：CORS 白名单 ----
      TRUSTED_CORS_ORIGINS: "http://localhost:5173,http://localhost:3001"
      # ---- 测试隔离：通知数据文件独立路径 ----
      NOTIFICATIONS_FILE: "/app/server/data-dev/notifications.json"
      # ---- 测试隔离：上传目录独立路径 ----
      VIDEO_UPLOAD_DIR: "/app/server/uploads-dev"
      # ---- 测试隔离：管理员账号 ----
      ADMIN_USERNAME: "admin"
      ADMIN_PASSWORD: "test-only-pwd"
    volumes:
      - ./server:/app/server
      - prohub_server_node_modules:/app/server/node_modules
    command: ["node", "index.js"]
    restart: unless-stopped
    depends_on:
      - rembg

  frontend:
    image: node:22-alpine
    container_name: prohub-frontend-dev
    working_dir: /app
    ports:
      - "5173:5173"
    environment:
      VITE_API_PROXY_TARGET: http://backend:3000
    volumes:
      - ./src:/app
      - prohub_frontend_node_modules:/app/node_modules
    command: ["sh", "-c", "npm install --no-audit --no-fund && npm run dev -- --host 0.0.0.0"]
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  prohub_server_node_modules:
  prohub_frontend_node_modules:






