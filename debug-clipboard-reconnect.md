# 剪切板页面刷新后持续重连调试记录

状态：[OPEN]
会话：clipboard-reconnect
开始日期：2026-08-31

## 用户现象

剪切板页面偶发在刷新后持续显示“重连”，最终显示“离线”。

## 可验证假设

1. 刷新时 roomId、clientId、hostToken 未同时恢复，导致连接请求缺少必要身份信息。
2. WebSocket 重连流程存在竞态，旧连接或旧定时器覆盖了新连接状态。
3. 剪切板页面初始化顺序异常，连接先于房间信息恢复执行。
4. 服务端房间已过期或鉴权失败，客户端未正确处理服务端错误。

## 运行时证据

本轮根据静态生命周期审查实施修复：

- 服务端 Host 断开后固定进入 15 秒恢复宽限期；宽限期内只有带有效 Host Token 的连接才会取消销毁计时。
- Guest 重新加入不会取消 Host 销毁计时，因此 Guest 在线不会继续保留无 Host 房间。
- 前端站内路由离开前，Host 使用已建立 Socket 和 Host Token 发送 `room:destroy`，等待最多 500ms ACK 后再清理连接。

## 修复结论

已完成最小修复并通过前端构建。Docker Compose 已重建并启动生产容器。仍需在浏览器验证：Host 刷新应在宽限期内恢复；Host 返回主页/切换工具应立即销毁；Guest 不能在 Host 离开后继续恢复历史列表。
